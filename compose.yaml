services:
  agent:
    build:
      context: ./apps/api
      dockerfile: agent/Dockerfile
    environment:
      AGENT_RUNTIME_HOST: 0.0.0.0
      AGENT_RUNTIME_PORT: 8765
      AGENT_RUNTIME_PYTHON_BASE_URL: http://api:8000
      AGENT_RUNTIME_INTERNAL_TOKEN: ${ADCRAFT_AGENT_RUNTIME_TOKEN:-adcraft-local-internal-token}
    init: true
    expose:
      - "8765"
    healthcheck:
      test:
        - CMD-SHELL
        - "node -e \"fetch('http://127.0.0.1:8765/internal/v1/health',{headers:{authorization:'Bearer '+process.env.AGENT_RUNTIME_INTERNAL_TOKEN}}).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    restart: unless-stopped
    networks:
      - adcraft-internal

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    environment:
      HOME: /tmp
      MEDIA_DATA_DIR: /app/data
      LOCAL_SETTINGS_ALLOWED_ORIGINS: "${ADCRAFT_PUBLIC_ORIGIN:-http://127.0.0.1:8080}"
      AGENT_RUNTIME_BASE_URL: http://agent:8765
      AGENT_RUNTIME_INTERNAL_TOKEN: ${ADCRAFT_AGENT_RUNTIME_TOKEN:-adcraft-local-internal-token}
    user: "${ADCRAFT_UID:-0}:${ADCRAFT_GID:-0}"
    init: true
    expose:
      - "8000"
    volumes:
      - ./apps/api:/app
      - ./runtime-data/api:/app/data
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health', timeout=5)"
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s
    depends_on:
      agent:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - adcraft-internal

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "${ADCRAFT_PORT:-8080}:80"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped
    networks:
      - adcraft-internal

networks:
  adcraft-internal:
